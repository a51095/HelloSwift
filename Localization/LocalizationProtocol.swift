protocol LocalizationProtocol {
    var commonYES: String { get }
    var commonSkip: String { get }
    var localizationTitle: String { get }
    var localizationMessage: String { get }
    var localizationDescription: String { get }
    var languageCode: LanguageCode { get }
}
